services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports: ["5050:5050"]
    env_file: [./backend/.env]
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports: ["5173:80"]
    depends_on: [backend]
